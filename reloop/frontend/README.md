# Reloop frontend

Vite + React + TypeScript + Tailwind CSS. See the top-level [`reloop/README.md`](../README.md)
for the full product overview, architecture, and deployment steps.

```bash
npm install
npm run dev      # http://localhost:5173, proxies /api to the local worker
npm run build    # outputs to dist/
```
