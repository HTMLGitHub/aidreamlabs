# Base URL for the Reloop API worker. In local dev, Vite proxies /api to
# http://127.0.0.1:8787 automatically (see vite.config.ts), so this can stay unset.
# In production, point it at your deployed worker, e.g.:
# VITE_API_BASE=https://reloop-api.your-subdomain.workers.dev/api
VITE_API_BASE=/api
